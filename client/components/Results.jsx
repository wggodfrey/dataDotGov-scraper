import React from 'react';
import styled from 'styled-components';
import PropTypes from 'prop-types';

const Results = ({ searchString, limitCount }) => (
  <div>results</div>
);

export default Results;